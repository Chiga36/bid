You are identifying every distinct tender question in a chunk of a UK public-sector tender
document, for a bid response tool. This chunk may be a section of a much larger document — it
may not run from the very start to the very end of the original file.

Document text:
"""
{document_text}
"""

Task: identify every distinct question or requirement a bidder must respond to in this text. For
each one:
- Give it a short title (a few words — reuse the heading it appears under, if there is one).
- Copy its full text verbatim from the document text above — the actual question/ask, including
  its own sub-requirements and any stated word/page/diagram limits. Do not include unrelated
  surrounding narrative.
- Classify it as exactly one of:
  - "sq" — a selection-questionnaire question: administrative, eligibility, or company-information,
    assessed pass/fail.
  - "pass_fail" — a mandatory technical requirement assessed pass/fail, not scored.
  - "scored" — a technical question that is scored or weighted.

Rules:
- Only extract genuine questions or requirements a bidder must respond to. Do NOT extract document
  titles, section headers with nothing actionable beneath them, general narrative about how the
  tender or evaluation process works, or anything else that does not itself require a response.
- Some documents number requirements hierarchically, e.g. "3.1" as a section heading with
  "3.1.1", "3.1.2", "3.1.3" underneath as the individual, actually-answerable questions. Extract
  at the individual-question (leaf) level, not the section-heading level — a bare section number
  with no question text of its own is not itself something to extract.
- A question's own weighting or pass/fail status is often stated inline, in parentheses right
  after its title — for example "Confirmation of Ability to Mobilise (Pass / Fail)" or
  "Understanding of Objectives and Risks (6%)". Treat "(Pass / Fail)" as a strong signal that the
  question is "pass_fail" rather than "scored" (unless it's clearly an SQ/eligibility question,
  in which case use "sq"). Keep this parenthetical as part of the verbatim question_text — it's
  part of the source text, not something to strip out.
- Every "question_text" value you return MUST be an exact, character-for-character substring of
  the document text above. If you cannot find a genuine verbatim match, omit it rather than
  inventing or paraphrasing one.
- If this chunk contains no genuine questions, return an empty list rather than forcing one.
