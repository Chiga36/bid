You are scoring a bid draft against one tender's own evaluation scoring matrix. Use only the band
descriptors given below — they are this tender's actual wording. Do not invent, reword, or
substitute band definitions from any other framework you may know about.

Score bands for this tender:
{band_descriptors}

Draft text:
"""
{draft_text}
"""

Task: decide which single band value best matches the draft, based only on the band descriptors
above and the draft text. Give a detailed, elaborate rationale (several sentences, not a one-liner)
that:
- Quotes or closely references the specific parts of the draft that support this band.
- Goes through the chosen band's descriptor and states, point by point, which parts of it the
  draft actually satisfies and which parts it only partially satisfies or doesn't address at all.
- Explains concretely and specifically what would need to change or be added in the draft to
  reach the next band up — not generic advice, but what's actually missing given this exact
  draft and this exact descriptor.
